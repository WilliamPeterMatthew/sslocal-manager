version: '3'

services:
  ssserver-libev:
    build: .
    container_name: ssserver-libev
    restart: always
    ports:
      - '8388:8388/udp'
      - '8388:8388/tcp'
    environment:
      - SERVER=0.0.0.0
      - SERVER_PORT=8388
      - PASSWORD=Password
      - TIMEOUT=300
      - METHOD=aes-256-gcm
      - DNS_ADDRS=114.114.114.114,8.8.8.8
